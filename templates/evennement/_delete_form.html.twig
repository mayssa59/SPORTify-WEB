<form method="post" action="{{ path('evennement_delete', {'idEvennement': evennement.idEvennement}) }}" onsubmit="return confirm('Are you sure you want to delete this event?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ evennement.idEvennement) }}">
    <br>
    <button class="btn btn-primary">Delete</button>
</form>
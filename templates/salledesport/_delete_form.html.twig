<form method="post" action="{{ path('Salle_delete', {'idSalle': Salle.idSalle}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ Salle.idSalle) }}">
    <br>
    <button class="btn btn-primary">Delete</button>
</form>
<form method="post" action="{{ path('zone_delete', {'region': zone.region}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ zone.region) }}">
    <br>
    <button class="btn btn-primary">Delete</button>
</form>

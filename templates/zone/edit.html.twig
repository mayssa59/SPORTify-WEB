{% extends 'template.html.twig' %}


{% block form %}
    <div style="text-align: center;">
        <h3>Edit Zone</h3>


        {{ include('zone/_formCours.html.twig', {'button_label': 'Update'}) }}

        <a class="btn btn-primary" href="{{ path('zone') }}">back to list</a>

        {{ include('zone/_delete_form.html.twig') }}
    </div>
{% endblock %}
